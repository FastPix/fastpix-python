"""
This file is auto-generated.
Do not edit this file manually.
Your changes will be overwritten during the next generation.
"""

from __future__ import annotations
from fastpix_python.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class PullVideoInputSegment2TypedDict(TypedDict):
    url: str
    r"""The URL of the **video segment** to be added.
    The URL must be **valid, publicly accessible, and downloadable** so that FastPix can retrieve and process the segment successfully.
    Supported video formats include **MP4, MOV, MKV, and TS** for best compatibility and performance.

    """
    insert_at_end: bool
    r"""Flag indicating the segment should be inserted at the end."""


class PullVideoInputSegment2(BaseModel):
    url: str
    r"""The URL of the **video segment** to be added.
    The URL must be **valid, publicly accessible, and downloadable** so that FastPix can retrieve and process the segment successfully.
    Supported video formats include **MP4, MOV, MKV, and TS** for best compatibility and performance.

    """

    insert_at_end: Annotated[bool, pydantic.Field(alias="insertAtEnd")]
    r"""Flag indicating the segment should be inserted at the end."""


class PullVideoInputSegment1TypedDict(TypedDict):
    url: str
    r"""The URL of the **video segment** to be added.
    The URL must be **valid, publicly accessible, and downloadable** so that FastPix can retrieve and process the segment successfully.
    Supported video formats include **MP4, MOV, MKV, and TS** for best compatibility and performance.

    """
    insert_at: int
    r"""The timestamp(in seconds) at which the segment must be inserted."""


class PullVideoInputSegment1(BaseModel):
    url: str
    r"""The URL of the **video segment** to be added.
    The URL must be **valid, publicly accessible, and downloadable** so that FastPix can retrieve and process the segment successfully.
    Supported video formats include **MP4, MOV, MKV, and TS** for best compatibility and performance.

    """

    insert_at: Annotated[int, pydantic.Field(alias="insertAt")]
    r"""The timestamp(in seconds) at which the segment must be inserted."""


PullVideoInputSegmentUnionTypedDict = TypeAliasType(
    "PullVideoInputSegmentUnionTypedDict",
    Union[PullVideoInputSegment1TypedDict, PullVideoInputSegment2TypedDict],
)


PullVideoInputSegmentUnion = TypeAliasType(
    "PullVideoInputSegmentUnion", Union[PullVideoInputSegment1, PullVideoInputSegment2]
)


class PullVideoInputTypedDict(TypedDict):
    type: NotRequired[str]
    r"""Defines the type of input.

    """
    url: NotRequired[str]
    r"""The URL hosts the media file for FastPix, which needs to be downloaded to use further. It supports formats like MP3, MP4, MOV, MKV, or TS, and includes text tracks for subtitles or closed captions (SRT/VTT files). The URL must be valid, publicly accessible, and downloadable to ensure FastPix can fetch the file successfully.

    While FastPix can handle various audio and video formats and codecs, using standard and widely supported formats helps achieve optimal processing speed.

    """
    start_time: NotRequired[int]
    r"""Start time indicates where encoding must begin within the video file. For example, if you want to encode a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600 seconds) video, the start time is 3 minutes (180 seconds). Note: Start time is always mentioned in seconds.

    """
    end_time: NotRequired[int]
    r"""End time indicates where encoding must end within the video file. For example, if you want to encode a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600 seconds) video, the end time is 6 minutes (360 seconds). Note: End time is always mentioned in seconds.

    """
    intro_url: NotRequired[str]
    r"""The URL of the **intro video** to be added at the beginning of the media file.
    The URL must be **valid, publicly accessible, and downloadable** so that FastPix can fetch the file successfully.
    Supported video formats include **MP4, MOV, MKV, and TS** for optimal processing performance.

    """
    outro_url: NotRequired[str]
    r"""The URL of the **outro video** to be added at the end of the media file.
    The URL must be **valid, publicly accessible, and downloadable** so that FastPix can retrieve the file successfully.
    Supported video formats include **MP4, MOV, MKV, and TS** for best compatibility and processing speed.

    """
    expunge_segments: NotRequired[List[str]]
    r"""The list of start and end times (in seconds) of the segments to be removed from the actual video.

    """
    segments: NotRequired[List[PullVideoInputSegmentUnionTypedDict]]
    r"""A list of media segments to be added or processed. Each segment includes details such as the URL of the media file and instructions on where it should be inserted in the final media composition. A segment can either specify an exact timestamp  (`insertAt`) or indicate that it must be added at the end (`insertAtEnd`)."""


class PullVideoInput(BaseModel):
    type: Optional[str] = "video"
    r"""Defines the type of input.

    """

    url: Optional[str] = "https://static.fastpix.io/fp-sample-video.mp4"
    r"""The URL hosts the media file for FastPix, which needs to be downloaded to use further. It supports formats like MP3, MP4, MOV, MKV, or TS, and includes text tracks for subtitles or closed captions (SRT/VTT files). The URL must be valid, publicly accessible, and downloadable to ensure FastPix can fetch the file successfully.

    While FastPix can handle various audio and video formats and codecs, using standard and widely supported formats helps achieve optimal processing speed.

    """

    start_time: Annotated[Optional[int], pydantic.Field(alias="startTime")] = None
    r"""Start time indicates where encoding must begin within the video file. For example, if you want to encode a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600 seconds) video, the start time is 3 minutes (180 seconds). Note: Start time is always mentioned in seconds.

    """

    end_time: Annotated[Optional[int], pydantic.Field(alias="endTime")] = None
    r"""End time indicates where encoding must end within the video file. For example, if you want to encode a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600 seconds) video, the end time is 6 minutes (360 seconds). Note: End time is always mentioned in seconds.

    """

    intro_url: Annotated[Optional[str], pydantic.Field(alias="introUrl")] = None
    r"""The URL of the **intro video** to be added at the beginning of the media file.
    The URL must be **valid, publicly accessible, and downloadable** so that FastPix can fetch the file successfully.
    Supported video formats include **MP4, MOV, MKV, and TS** for optimal processing performance.

    """

    outro_url: Annotated[Optional[str], pydantic.Field(alias="outroUrl")] = None
    r"""The URL of the **outro video** to be added at the end of the media file.
    The URL must be **valid, publicly accessible, and downloadable** so that FastPix can retrieve the file successfully.
    Supported video formats include **MP4, MOV, MKV, and TS** for best compatibility and processing speed.

    """

    expunge_segments: Annotated[
        Optional[List[str]], pydantic.Field(alias="expungeSegments")
    ] = None
    r"""The list of start and end times (in seconds) of the segments to be removed from the actual video.

    """

    segments: Optional[List[PullVideoInputSegmentUnion]] = None
    r"""A list of media segments to be added or processed. Each segment includes details such as the URL of the media file and instructions on where it should be inserted in the final media composition. A segment can either specify an exact timestamp  (`insertAt`) or indicate that it must be added at the end (`insertAtEnd`)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "type",
                "url",
                "startTime",
                "endTime",
                "introUrl",
                "outroUrl",
                "expungeSegments",
                "segments",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
